<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- Цвет фона -->
    <stroke
        android:width="4dp"
        android:color="@color/white"/> <!-- Цвет обводки -->
    <corners android:radius="3dp"/> <!-- Закругление углов (по желанию) -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp"/>
</shape>
